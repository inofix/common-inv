classes:
  - app.postgresql

applications:
  - postgresql-server

parameters:
  app__postgresql__config: "/etc/postgresql/${app__postgresql__version}/main/postgresql.conf"
  app__postgresql__hba: "/etc/postgresql/${app__postgresql__version}/main/pg_hba.conf"
